package ru.you11.repasttestproject.model

data class Tips(val id: Int,
                val waiterId: Int,
                val size: Int,
                val userId: Int,
                val restId: Int) {
}